import { Color } from 'three';

// Green swatch at UV (0.2795, 0.7068) in the bush's existing palette texture.
// Used only to compensate seasonal vertex colours; the texture stays intact.
export const bushTextureColor = new Color('#608020');
