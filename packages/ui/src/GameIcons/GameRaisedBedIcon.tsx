import { RaisedBedIcon, type RaisedBedIconProps } from '../RaisedBedIcon';

/** Shares the established physical-identifier layout with the outline icon. */
export function GameRaisedBedIcon(props: RaisedBedIconProps) {
    return <RaisedBedIcon {...props} appearance="game" />;
}
