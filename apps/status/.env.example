CHECKLY_API_KEY=
# Optional when the API key only has one Checkly account.
CHECKLY_ACCOUNT_ID=
CHECKLY_STATUS_TAG=gredice-status
# Read-only PostgreSQL role with column-level access to events.id,
# events.type and events.created_at only.
GREDICE_LIVE_DATABASE_URL=
