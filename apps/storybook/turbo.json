{
    "$schema": "https://turbo.build/schema.json",
    "extends": ["//"],
    "tasks": {
        "build": {
            "dependsOn": ["^typecheck"],
            "outputs": ["storybook-static/**"]
        }
    }
}
