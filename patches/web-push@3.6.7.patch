diff --git a/src/web-push-lib.js b/src/web-push-lib.js
index a47df05..d365f49 100644
--- a/src/web-push-lib.js
+++ b/src/web-push-lib.js
@@ -3 +2,0 @@
-const url = require('url');
@@ -274 +273 @@ WebPushLib.prototype.generateRequestDetails = function(subscription, payload, op
-      const parsedUrl = url.parse(subscription.endpoint);
+      const parsedUrl = new URL(subscription.endpoint);
@@ -348,4 +347,6 @@ WebPushLib.prototype.sendNotification = function(subscription, payload, options)
-      const urlParts = url.parse(requestDetails.endpoint);
-      httpsOptions.hostname = urlParts.hostname;
+      const urlParts = new URL(requestDetails.endpoint);
+      httpsOptions.hostname = urlParts.hostname.startsWith('[')
+        ? urlParts.hostname.slice(1, -1)
+        : urlParts.hostname;
       httpsOptions.port = urlParts.port;
-      httpsOptions.path = urlParts.path;
+      httpsOptions.path = urlParts.pathname + urlParts.search;
